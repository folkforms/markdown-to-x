const execute = (input, mappings) => {
    console.log(`---- mappings ----`);
    console.log(`input = ${input}`);
    console.log(`mappings = ${mappings}`);
    throw new Error(`ERROR: Mappings not implemented yet!`);
  };
  
  module.exports = { execute };
  